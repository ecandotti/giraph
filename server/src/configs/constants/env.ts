// Environment mode
export const NODE_ENV = process.env.NODE_ENV;

// URI connexion to database
export const DB_URI = process.env.DATABASE_URL;

// Mailer
export const SMTP_HOST = process.env.SMTP_HOST;
export const SEND_MAIL = process.env.SEND_MAIL;
export const PASS_MAIL = process.env.PASS_MAIL;

// Server
export const PORT = process.env.PORT;

// JWT
export const JWT_SECRET = process.env.JWT_SECRET;

// App credentials
export const URL_FRONT = process.env.URL_FRONT;

// Apple credentials
export const MAIL_TEST = process.env.MAIL_TEST;
